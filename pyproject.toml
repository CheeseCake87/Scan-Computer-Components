[project]
name = "scan-computer-components"
version = "0.1.6"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.2",
    "pyqwe>=3.1.1",
    "requests>=2.32.5",
    "ruff>=0.13.3",
]

[tool.pyqwe]
run = "*:python3 -m fetcher"
daily = [
    "@sync",
    "*:python3 -m fetcher",
    "*:git add pricing/pricing_data.json",
    "*:git commit -m 'update pricing'",
    "*:git push",
]
